apiVersion: v1
kind: Service
metadata:
  namespace: kongair-flights
  labels:
    app: kongair-flights-service
  name: kongair-flights-service
spec:
  ports:
  - name: 8080-8080
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: kongair-flights
  type: ClusterIP
status: